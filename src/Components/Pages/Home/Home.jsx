import React from 'react';
import ImageCarousel from '../ImageCarousel/ImageCarousel';

const Home = () => {
    return (
        <div>
            <ImageCarousel/>
        </div>
    );
};

export default Home;